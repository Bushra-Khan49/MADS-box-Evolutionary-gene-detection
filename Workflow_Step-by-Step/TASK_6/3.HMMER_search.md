Step 3: Run HMMER search

Aim To identify MADS and K-domain proteins using Pfam-calibrated HMM profiles sourced from UniProt/InterPro.

Procedure

Navigate to Project Directory:

cd ~/Desktop/NIPGR-data/HMMER_project
Execute HMMER Search:

# Perform hmmsearch for MADS and K-domain using Pfam profiles (Sourced from UniProt/InterPro)
hmmsearch --tblout {species_fa}_MADS_hmmsearch_results.txt PF00319.MADS.hmm {species_fa}.fa
hmmsearch --tblout {species_fa}_K_domain_hmmsearch_results.txt PF01486.K_domain.hmm {species_fa}.fa
HMMER Results

Results Preview

```txt
{raw_previews[0] if len(raw_previews) > 0 else '# hmmsearch output...'}
```

Step 3.1: Merge MADS and K_domain results

Merge Domain Results:
cd ~/Desktop/NIPGR-data/HMMER_project
cat {species_fa}_MADS_hmmsearch_results.txt {species_fa}_K_domain_hmmsearch_results.txt > Combined_hmmsearch_results(MADS+K_domain).txt
Combined hmm results

Results Preview

```txt
{raw_previews[1] if len(raw_previews) > 1 else '# Merged results...'}
```

Step 3.2: Shortlist candidates (Combining BLAST and HMMER)

Combine Analytical Results:
cd ~/Desktop/NIPGR-data/HMMER_project

# Combine finalists from blast and domain searches
cat Combined_hmmsearch_results\(MADS+K_domain\).txt ../BLAST_project/{species_fa}_Blast_results.txt > Shortlisted_candidates.txt
head -n 30 Shortlisted_candidates.txt
Shortlisted candidates

Results Preview

```txt
{raw_previews[2] if len(raw_previews) > 2 else '# Shortlisted candidates...'}
```
------Candidates Shortlisted------
