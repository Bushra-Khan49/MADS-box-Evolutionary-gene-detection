# <h2 style="color:darkblue; font:Times New Roman;">BLAST Workflow — Step 01 (Arabidopsis → Rice example)
</h2> 
<br>

## **Aim**  
Make a BLAST DB from a species proteome (here: Oryza/rice), query Arabidopsis ABCDE genes, perform blast search; remove redundancies; peform self blast from obtained results; collect unique orthologus genes and visualize mapping statistics.
<br>

## Steps:-

```bash
#!/bin/bash
```
> ### Step 0: Navigate to project directory
```
cd /Users/bushrakhan/Desktop/NIPGR-data/BLAST_project
pwd
ls -l
```

> ### Step 1: Create BLAST database
```
makeblastdb -in Osativa_323_v7.0.protein.fa \
            -dbtype prot \
            -parse_seqids \
            -out rice_db
```
[Rice databse output file]( 
>### Step 2: BLASTP Arabidopsis queries vs rice DB
```
blastp -query At_ABCDE_QUERY.fa \
       -db rice_db \
       -evalue 1e-5 \
       -outfmt 7 \
       -out blast_results.out \
       -num_threads 4
```

> ### Step 3: Verify BLAST results
```
head -n 20 blast_results.out
```

> ### Step 4: Extract unique rice hit IDs
```
awk '!/^#/ {print $2}' blast_results.out | sort | uniq > rice_hits_ids.txt
```

> ### Step 5: Fetch FASTA sequences of hits
```
blastdbcmd -db rice_db \
           -entry_batch rice_hits_ids.txt \
           -out rice_hits.fa
```

> ### Step 6: Self-BLAST rice hits
```
blastp -query rice_hits.fa \
       -db rice_db \
       -evalue 1e-5 \
       -outfmt 7 \
       -out rice_hits_selfblast.out \
       -num_threads 4
```

> ### Step 7: Verify self-BLAST results
```
head -n 20 rice_hits_selfblast.out
```
<br>
## <p align="center">------ Workflow Complete ------</p>

<br><br>




# Visualisation using pandas and matplotlib in Python

```python```
> ### Step 1: Upload your file
```
from google.colab import files
import pandas as pd
import matplotlib.pyplot as plt
```

> ### Step 2: Load file
```df = pd.read_csv("Blast results corrected.csv")```

> ### Step 3: Process columns
```
df['Query_Gene'] = df['Query ID'].astype(str).str.split().str[-1]
df['Subject_Gene'] = df['Subject ID'].astype(str)

df = df.dropna(subset=['Query_Gene', 'Subject_Gene'])
df = df[(df['Query_Gene'] != "nan") & (df['Subject_Gene'] != "nan")]

df['Bit Score'] = pd.to_numeric(df['Bit Score'], errors='coerce')
df['% Identity'] = pd.to_numeric(df['% Identity'], errors='coerce')
df['E-value'] = pd.to_numeric(df['E-value'], errors='coerce')
df = df.dropna(subset=['Bit Score', '% Identity', 'E-value'])
```

> ### Step 4: Map gene names to numeric positions for plotting
```
x_labels = df['Query_Gene'].unique()
y_labels = df['Subject_Gene'].unique()

x_map = {label: i for i, label in enumerate(x_labels)}
y_map = {label: i for i, label in enumerate(y_labels)}

plt.figure(figsize=(14,10))
```

> ### Step 5: Plot scatter with mapped positions
```
scatter = plt.scatter(
    df['Query_Gene'].map(x_map),
    df['Subject_Gene'].map(y_map),
    s=df['Bit Score']/2,
    c=df['% Identity'],
    cmap="viridis", alpha=0.7, edgecolors="k"
)

plt.colorbar(scatter, label="% Identity")
plt.xticks(list(x_map.values()), list(x_map.keys()), rotation=45, ha='right')
plt.yticks(list(y_map.values()), list(y_map.keys()))
plt.xlabel("Arabidopsis Genes (Query)")
plt.ylabel("Oryza Genes (Subject)")
plt.title("Arabidopsis vs Oryza BLAST Comparison")
```

> ### Step 6: Annotate with E-value to the LEFT of each bubble
```
for i, row in df.iterrows():
    plt.text(
        x_map[row['Query_Gene']] - 0.3,
        y_map[row['Subject_Gene']],
        f"{row['E-value']:.1e}",
        fontsize=6, ha='right', va='center', color="black"
    )

plt.show()

uploaded = files.upload()
```
> ## Visualise Results
<br>

## <p align="center"> ------Visualisation Complete------ </p>
<br><br>

---
